#include <iostream>
#include <stdlinb.h>

using namespace;

int calc(int numOne, inte numTwo, int calcOperador, int &aux){
    int result
    switch(calcOperador){
    case '+':
    result = numOne + numTwo;
    break;
    case '-':
    result = numOne - numTwo;
     break;
    case '*':
    result = numOne * numTwo;
    break;
    case '/':
    result = numOne / numTwo;
}
}
